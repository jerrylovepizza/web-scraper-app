<!-- ======================================= Nav ======================================= -->
<nav class="navbar navbar-expand-lg navbar-light bg-light" id="main">
    <img src="/css/original.gif" class="panda" alt="">
    <a href="/" class="header" style="text-decoration:none">IGN Scraper </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
        aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a href="/" class="btn btn-light btn-sm navBtn" id="homePgBtn">Home</a>
            </li>
            <li class="nav-item">
                <a href="/saved" class="btn btn-light btn-sm navBtn" id="savePgBtn">Saved</a>
            </li>
            <li class="nav-item">
                <a href="#" class="btn btn-light btn-sm navBtn" id="scrapeBtn">Scrape News</a>
            </li>
            <li class="nav-item">
                <a href="#" class="btn btn-light btn-sm navBtn" id="clearBtn">Clear News</a>
            </li>
        </ul>
    </div>
    <span class='smile' data-switch='off'>💡</span><img src="/css/s3iCH.gif" class="star" alt="Welcome!!">
</nav>

<!-- ======================================= Body ======================================= -->
<div class="container newsList">
    <div class="row">
        {{#each news}}
        <div class="list-group col-lg-5 col-12 m-auto" id="news">
            <a href="{{this.link}}" id="ignLink" target="_blank">
                <div class="news-title">{{this.title}}</div>
                <img id="thumb" src="{{this.img}}">
            </a>
            <button class="saveBtn btn-danger" data-ObjectId="{{this._id}}" data-Title="{{this.title}}"
                data-State="{{this.saved}}">
                <i class="fas fa-heart"></i>
            </button>
        </div>
        {{/each}}
    </div>
</div>

<!-- ======================================= footer ======================================= -->
<footer>
    <hr>
    <span>Copyright &copy; 2019 Isaac Wu</span>
    <span> · Powered by IGN.com</sp>
</footer>